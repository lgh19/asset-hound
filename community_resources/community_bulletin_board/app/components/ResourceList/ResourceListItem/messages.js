/*
 * ResourceListItem Messages
 *
 * This contains all the text for the ResourceListItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ResourceListItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ResourceListItem component!',
  },
});
