/*
 * ResourceList Messages
 *
 * This contains all the text for the ResourceList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ResourceList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ResourceList component!',
  },
});
