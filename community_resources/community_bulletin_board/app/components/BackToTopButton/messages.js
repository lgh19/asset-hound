/*
 * BackToTopButton Messages
 *
 * This contains all the text for the BackToTopButton component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BackToTopButton';

export default defineMessages({
  top: {
    id: `${scope}.top`,
    defaultMessage: 'Back to Top',
  },
});
