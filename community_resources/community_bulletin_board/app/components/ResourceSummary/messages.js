/*
 * ResourceSummary Messages
 *
 * This contains all the text for the ResourceSummary component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ResourceSummary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ResourceSummary component!',
  },
});
