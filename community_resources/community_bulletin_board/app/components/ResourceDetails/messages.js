/*
 * ResourceDetails Messages
 *
 * This contains all the text for the ResourceDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ResourceDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ResourceDetails component!',
  },
});
