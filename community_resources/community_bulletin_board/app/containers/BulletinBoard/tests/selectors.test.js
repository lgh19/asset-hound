// import { selectBulletinBoardDomain } from '../selectors';

describe('selectBulletinBoardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
