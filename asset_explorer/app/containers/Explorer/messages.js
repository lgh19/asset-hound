/*
 * Explorer Messages
 *
 * This contains all the text for the Explorer container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Explorer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Explorer container!',
  },
});
